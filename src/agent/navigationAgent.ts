/**
 * Natural-language navigation agent — re-exports the shared agent loop.
 * Accessibility repair is primary; this is the secondary interface.
 */
export { runAgentTurn } from "./agent";
export type { PageToolExecutor } from "./agent";
